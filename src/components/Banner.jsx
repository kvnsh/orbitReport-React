// Create a new file named Banner.jsx inside the components directory.

// Inside this file, create a function called Banner.

// This function should return a <header> tag that says: “Orbit Report”.

// Below the <header> create a <p> tag with instructions for users.

// Here is an example message:

// Click on the buttons to see the satellites in that orbit type
// Make sure that you are exporting this function.

function Banner() {
    return (
      <header>
        <h1>Orbit Report</h1>
        <p>Click on the buttons to see the satellites in that orbit type.</p>
      </header>
    );
  }
  
  export default Banner;